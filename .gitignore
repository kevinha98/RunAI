node_modules
.next
dist
.expo
.turbo
*.local
.env
.env.*
!.env.example

# Strava stats store (personal data — not for version control)
apps/web/data/

autonomous/

# TypeScript incremental build info
*.tsbuildinfo
